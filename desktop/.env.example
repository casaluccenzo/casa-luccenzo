# Token de GitHub con scope public_repo, para publicar releases.
# Copiar este archivo a desktop/.env (gitignored) con el valor real antes de
# `npm run publish`. electron-builder lo lee automaticamente.
GH_TOKEN=
