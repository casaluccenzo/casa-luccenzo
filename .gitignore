# System files
Thumbs.db
Desktop.ini
.DS_Store

# IDE and Editor files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Project files and logs
.gemini/
*.log

# Supabase configuration (to avoid exposing private keys if they are changed, though anon key is public)
# It is best practice to keep local configuration files untracked if they differ by environment.
js/supabaseConfig.js

# Dependencies and build folders
node_modules/
www/

.vercel
.env*
supabase/.temp
.staging-db-password.txt

# Baileys WhatsApp auth state -- these files are equivalent to being logged in
# to the WhatsApp account. Never commit them.
whatsapp-session/

# Isolated git worktrees used for subagent-driven-development plan execution.
.worktrees/

# Claude Code skill tooling (installed locally, not part of the app)
.agents/
.claude/skills/
skills-lock.json
.gstack/
.codex/
.github/agents/
.github/hooks/
.github/skills/
.impeccable/

# Visual brainstorming companion (superpowers)
.superpowers/
